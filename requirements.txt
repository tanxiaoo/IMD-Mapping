# Google Earth Engine
earthengine-api
geemap
geedim

# Geospatial
rasterio
rioxarray
geopandas
fiona
shapely
pyproj

# ML / data
scikit-learn
scipy
pandas
numpy
joblib

# Plotting
matplotlib
seaborn

# Notebook / utils
jupyter
ipykernel
tqdm

# Report export (code/export_docx.py)
# Pillow is used to read figure dimensions so each image is placed at a
# sensible width rather than its native size.
python-docx
pillow
